{
  "name": "ivac-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Job-isolated automation platform with durable state, exclusive IP allocation, workflow execution, and operational dashboard.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/config/schema.js && node --check src/config/loader.js && node --check src/core/errors.js && node --check src/core/logger.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pino": "^9.0.0",
    "zod": "^3.23.8"
  }
}